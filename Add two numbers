#include <stdio.h>

int main() {
    int num1, num2, sum;

    // Taking input
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    // Adding numbers
    sum = num1 + num2;

    // Displaying result
    printf("Sum = %d\n", sum);

    return 0;
}
